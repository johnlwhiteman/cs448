#!/usr/bin/env bash
export DIR="$(cd "$(dirname "${BASH_SOURCE[0]}" )" && pwd)"
source ${DIR}/libSetup

function setupDependencies {
    setupGit
    setupVim
}

sudo ls >/dev/null
setupLinux
setupDependencies
